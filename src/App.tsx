import { Component } from 'solid-js';

const App: Component = () => {
    return (
        <button class="btn w-full">
            <i class="bi bi-2-circle"></i>
            hello
        </button>
    );
};

export default App;
